import {StyleSheet} from 'react-native';
import {Colors} from '../../theme/colors';
const intakesStyles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: Colors.primary,
    paddingHorizontal: 10,
  },
});

export {intakesStyles};
